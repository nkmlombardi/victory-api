const bcrypt = require('bcryptjs')
const chance = require('chance')()
const random_ip = chance.ip()
const ip_hash = bcrypt.hashSync(random_ip, 10)


module.exports = {
    secretKey: 'onelinkSecret',
    random_ip: random_ip,
    ip_hash: ip_hash
}
