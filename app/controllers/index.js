module.exports = {
    // Resources
    user: require('./user'),
    transaction: require('./transaction'),

    // Authentication Resources
    auth: require('./authentication'),
    token: require('./token')
};
